{
  "name": "students-keeper-app-part-1-starting-forked",
  "version": "1.0.0",
  "description": "A note keeping app with React frontend and Express backend",
  "keywords": [],
  "main": "server.js",
  "dependencies": {
    "express": "^4.19.2",
    "react": "16.8.6",
    "react-dom": "16.8.6",
    "react-scripts": "3.2.0",
    "concurrently": "^6.2.0"
  },
  "devDependencies": {
    "typescript": "3.3.3"
  },
  "scripts": {
    "start": "concurrently \"NODE_OPTIONS=--openssl-legacy-provider react-scripts start\" \"node server.js\"",
    "build": "NODE_OPTIONS=--openssl-legacy-provider react-scripts build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
